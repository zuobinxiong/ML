import logRegres
from numpy import *

# dataSet, labelMat = logRegres.loadDataSet()
# weights = logRegres.gradAscent(dataSet, labelMat)
# weights = logRegres.stocGradAscent(array(dataSet), labelMat)
# weights = logRegres.stocGradAscent1(array(dataSet), labelMat)
# logRegres.plotBestFit(weights)
logRegres.multiTest()